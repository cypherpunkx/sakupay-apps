package constants

const (
	WHERE_BY_ID                  = "id = ?"
	WHERE_BY_USER_ID             = "user_id = ?"
	WHERE_BY_BILL_ID             = "bill_id = ?"
	WHERE_BY_USER_ID_AND_CARD_ID = "user_id = ? AND = id = ?"
)
